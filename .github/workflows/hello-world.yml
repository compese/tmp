name: Hello World

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Echo Hello World
        run: echo "Commit message: testin" #${{ github.event.head_commit.message }}"
